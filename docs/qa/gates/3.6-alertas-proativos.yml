schema: 1
story: '3.6'
slug: alertas-proativos
title: Alertas Proativos de Saldo via WhatsApp
gate: PASS
status_reason: >-
  Re-gate após FAIL da 1ª rodada. Os 3 bloqueadores corrigidos e verificados na
  infraestrutura real (REST com service_role + curl no dev server): BLOCK-001
  coluna account_name (query cron 200; contra-prova com `name` → 400); BLOCK-002
  grants service_role via migration 000017 (4 tabelas 403→200); QA-3 proxy.ts
  liberando rotas de serviço do gate de login (send-alerts 401/401/200
  {"processed":0}). Bônus verificado: crons do Epic 2 destravados (401, não mais
  307) e gate de sessão intacto. 12/12 testes com mock fiel ao schema.
reviewer: 'Quinn'
updated: '2026-07-07T14:15:00Z'
previous_gate:
  verdict: FAIL
  date: '2026-07-07T13:35:00Z'
  blockers: ['BLOCK-001 coluna name inexistente', 'BLOCK-002 alerts sem grant service_role']

acceptance_criteria:
  - id: 'AC-3.6.1'
    status: met
    note: >-
      Guard 401 verificado por curl (sem header e secret errado → 401); busca de
      alertas com ad_accounts!inner(account_name) → 200 na infraestrutura real.
  - id: 'AC-3.6.2'
    status: met
    note: >-
      Claim atômico UPDATE ... WHERE whatsapp_sent_at IS NULL RETURNING id;
      GRANT UPDATE em alerts aplicado (000017) — executável. Teste de concorrência
      (claim perdido → skip) PASS.
  - id: 'AC-3.6.3'
    status: met
    note: >-
      Rollback whatsapp_sent_at = NULL + whatsapp_last_error nos dois caminhos de
      falha (não-ok e exceção). Testes PASS; grants permitem o UPDATE.
  - id: 'AC-3.6.4'
    status: met
    note: "Worker suporta @g.us (fix 3.5 T1); validação /^\\d+@g\\.us$/ presente."
  - id: 'AC-3.6.5'
    status: met
    note: >-
      Alerta sem destino: workspace ausente no Map → skipped, sem claim, sem log de
      erro. Teste PASS.
  - id: 'AC-3.6.6'
    status: met
    note: 'AlertDestinationForm na seção WhatsApp; radio individual/grupo; validação client+server.'
  - id: 'AC-3.6.7'
    status: met
    note: >-
      saveAlertDestination valida formato pré-auth; UPDATE filtra id + workspace_id
      (IDOR). Testes PASS.
  - id: 'AC-3.6.8'
    status: met
    note: >-
      Mensagem usa adAccount.account_name (coluna real); formato com nome da conta,
      projeção, limite e data sugerida. Teste de conteúdo PASS.
  - id: 'AC-3.6.9'
    status: met
    note: "vercel.json com /api/cron/send-alerts schedule '*/15 * * * *'."
  - id: 'AC-3.6.10'
    status: met
    note: >-
      12/12 testes PASS com mock fiel ao schema ({ account_name }). Cenários:
      claim ok/worker ok, claim ok/worker fail (rollback), claim perdido, sem destino.

evidence:
  tests_run: 12
  tests_passed: 12
  test_command: 'pnpm --filter @advezo/web exec vitest run alert-destination'
  manual_test: >-
    REST service_role: grants 200 em ad_accounts/ad_campaigns/campaign_metrics/alerts;
    query exata do cron (account_name) 200; contra-prova (name) 400.
    Curl dev server: send-alerts 401 (sem header) / 401 (secret errado) / 200
    {"processed":0} (autenticado); send-reports 200; sync/meta 401; sync/google 401;
    alerts/detect 401 (todos eram 307 antes do fix QA-3); /dashboard e /clients
    307 → /login (gate de sessão intacto).
  typecheck: 'PASS'
  regression: '321 passed na suíte completa; falha workspace-actions.test.ts pré-existente'
  files_reviewed:
    - apps/web/src/app/api/cron/send-alerts/route.ts
    - apps/web/src/proxy.ts
    - apps/web/src/__tests__/alert-destination.test.ts
    - supabase/migrations/20260101000017_epic2_grants_service_role.sql

quality_checks:
  code_review: PASS
  unit_tests: PASS
  acceptance_criteria: PASS
  no_regressions: PASS
  performance: PASS
  security: PASS
  documentation: PASS

top_issues:
  - id: 'OBS-001'
    severity: low
    finding: >-
      Auth do cron por comparação direta !== `Bearer ${CRON_SECRET}` — se a env var
      faltar, "Bearer undefined" passaria (fail-open teórico). Vercel Cron só envia
      o header quando a var existe e o deploy checklist exige configurá-la.
    suggested_action: >-
      Alinhar com o guard fail-closed das rotas do Epic 2
      (if (!cronSecret || !provided || provided !== cronSecret)) em manutenção
      futura. Não bloqueia.

side_effects_verified:
  - 'Crons do Epic 2/2.9 destravados pelo fix QA-3 (sync/meta, sync/google, alerts/detect → 401).'
  - 'TD-002 atualizado e TD-004 registrado em docs/tech-debt.md (/api/leads/submit — dono Story 8.3).'

waiver: { active: false }
