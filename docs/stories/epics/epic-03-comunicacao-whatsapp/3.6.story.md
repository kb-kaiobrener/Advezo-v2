# Story 3.6 — Alertas Proativos de Saldo via WhatsApp

## Status

**Status:** InReview
**Epic:** 03 — Comunicação com Cliente via WhatsApp
**Wave:** 4 — Envio real de relatório + alertas proativos
**Depende de:** Story 2.9 Done (tabela `alerts` com dedup por índice único parcial), Story 3.1 Done (worker Baileys), Story 3.2 Done (conexão WhatsApp + conta ativa)
**Worker group JID fix:** Aplicado pela Story 3.5 (T1) — confirmar antes de implementar T1 desta story.
**Criada em:** 2026-07-06

## Executor Assignment

| Role | Agent | Notes |
|------|-------|-------|
| Executor | @dev (Dex) | Modo interativo — checkpoints em T3, T5, T6 |
| Quality Gate | @qa (Quinn) | Gate completo — 10 ACs; atenção especial ao dedup (AC 3.6.2) e ao fluxo retry (AC 3.6.3) |

---

## Story

**As a** gestor de tráfego,
**I want to** receber alerta via WhatsApp quando o saldo de uma conta estiver projetado para esgotar em menos de N dias,
**so that** eu possa agir antes da campanha ser pausada por saldo insuficiente.

---

## Acceptance Criteria

- [x] **AC 3.6.1** — Cron route `GET /api/cron/send-alerts` (Vercel Cron, a cada 15 min `*/15 * * * *`): protegida por `Authorization: Bearer ${CRON_SECRET}` (401 se ausente ou inválido). Busca alertas com `resolved_at IS NULL AND whatsapp_sent_at IS NULL`. Para cada alerta, obtém a conta WhatsApp conectada do workspace (`whatsapp_accounts` com `status = 'connected'`) e o destino configurado (`alert_destination_id`).

- [x] **AC 3.6.2** — Dedup via claim atômico: antes de chamar o worker, executa `UPDATE alerts SET whatsapp_sent_at = NOW(), whatsapp_destination_id = $destination WHERE id = $id AND whatsapp_sent_at IS NULL RETURNING id`. Se `RETURNING` retornar vazio (outro processo já enviou), skip silencioso. Garante que o mesmo alerta nunca é enviado duas vezes, mesmo em cenário de retry ou reinício do cron.

- [x] **AC 3.6.3** — Falha no worker: executa `UPDATE alerts SET whatsapp_sent_at = NULL, whatsapp_last_error = $erro WHERE id = $id` (desfaz o claim). Alerta fica com `whatsapp_sent_at IS NULL` e será retentado no próximo ciclo do cron. `whatsapp_last_error` preserva o último erro para diagnóstico.

- [x] **AC 3.6.4** — Suporte a grupo: destinatário pode ser JID `@g.us`. O worker já suporta grupos após o fix da Story 3.5 (T1). A validação de formato é idêntica à das Stories 3.3 e 3.5: `/^\d+@g\.us$/` para grupos, `/^\d{10,15}$/` para individuais.

- [x] **AC 3.6.5** — Alertas sem destino configurado (`whatsapp_accounts.alert_destination_id IS NULL`) são ignorados silenciosamente pelo cron (sem log de erro — é estado esperado para contas não configuradas).

- [x] **AC 3.6.6** — UI: sub-seção "Destino de Alertas" na página `/clients/[id]/configuracoes`, dentro da seção de configuração do WhatsApp (abaixo do status de conexão existente). Campos: tipo de destinatário (radio: individual / grupo) e ID do destinatário. Mesma validação client-side e server-side da Story 3.3 (individual: E.164; grupo: JID `@g.us`). Salvamento via Server Action `saveAlertDestination`.

- [x] **AC 3.6.7** — Server Action `saveAlertDestination(accountId: string, data: { destination_type: 'individual' | 'group'; destination_id: string })`: valida formato de `destination_id` server-side antes de auth; verifica membership + que a conta pertence ao workspace; executa `UPDATE whatsapp_accounts SET alert_destination_type, alert_destination_id WHERE id = $accountId AND workspace_id = $workspaceId`; `revalidatePath`.

- [x] **AC 3.6.8** — Formato da mensagem de alerta (texto puro, compatível com WhatsApp):
  ```
  ⚠️ Alerta de Saldo — {ad_account_name}
  Saldo projetado para esgotar em {projected_days} dias.
  Limite configurado: {threshold_days} dias de veiculação.
  Ação sugerida: recarregue o saldo da conta antes de {data_projetada}.
  ```
  `data_projetada` = hoje + `projected_days` dias, formatado `DD/MM/YYYY`.

- [x] **AC 3.6.9** — `vercel.json` em `apps/web/` (criado pela Story 3.5) atualizado com entrada para `/api/cron/send-alerts` com schedule `"*/15 * * * *"`.

- [x] **AC 3.6.10** — Testes unitários cobrendo: `saveAlertDestination` (validação pré-auth; IDOR check; upsert correto), cron handler (mock: claim bem-sucedido → worker ok → sent_at set; claim bem-sucedido → worker fail → rollback sent_at + last_error set; claim falha → skip; alert sem destino → skip).

---

## Tasks

- [x] **T1** — Verificar que a Story 3.5 (T1) já aplicou o fix do worker (group JID) — se não, aplicar antes de prosseguir
- [x] **T2** — Criar migration `supabase/migrations/20260101000016_alerts_whatsapp.sql`: adicionar colunas à `alerts` e `whatsapp_accounts` (ver Dev Notes)
- [x] **T3** — Aplicar migration: `supabase db push` + confirmar `supabase migration list` (Local=Remote até 000016)
- [x] **T4** — Implementar Server Action `saveAlertDestination` em `apps/web/src/app/actions/alert-destination.ts`
- [x] **T5** — Criar cron route `apps/web/src/app/api/cron/send-alerts/route.ts`: auth CRON_SECRET, claim atômico, worker call, rollback em falha (ver Dev Notes)
- [x] **T6** — UI: componente `AlertDestinationForm` em `apps/web/src/components/molecules/AlertDestinationForm.tsx` — campos tipo + ID, validação client-side idêntica à 3.3
- [x] **T7** — Integrar `AlertDestinationForm` em `/clients/[id]/configuracoes/page.tsx`: busca `whatsapp_accounts` com `alert_destination_type` e `alert_destination_id`; passa como prop para o form
- [x] **T8** — Adicionar entrada `send-alerts` em `apps/web/vercel.json` (criado pela 3.5)
- [x] **T9** — Testes unitários em `apps/web/src/__tests__/alert-destination.test.ts` (AC 3.6.10)
- [x] **T10** — Atualizar story: checkboxes, Dev Agent Record (File List), Change Log

---

## Dev Notes

### Migration 000016 — alerts + whatsapp_accounts

```sql
-- ============================================================
-- Migration: 20260101000016_alerts_whatsapp  (logical number 000016)
-- Purpose: Tracking de envio de alertas via WhatsApp (Story 3.6)
-- Notas:
--  - whatsapp_sent_at: timestamp de envio confirmado. NULL = não enviado.
--    Usado como claim atômico no cron (UPDATE WHERE whatsapp_sent_at IS NULL).
--  - whatsapp_last_error: último erro de envio — preservado para diagnóstico;
--    limpo a NULL em envio bem-sucedido.
--  - alert_destination_type/id em whatsapp_accounts: destino de alertas por conta,
--    independente do destination_id de relatórios (report_schedules).
-- ============================================================

ALTER TABLE public.alerts
  ADD COLUMN whatsapp_sent_at       timestamptz,
  ADD COLUMN whatsapp_destination_id text,
  ADD COLUMN whatsapp_last_error    text;

-- Índice para o cron — alertas ativos sem envio (caminho quente, cada 15 min)
CREATE INDEX alerts_whatsapp_pending_idx
  ON public.alerts (workspace_id, created_at DESC)
  WHERE resolved_at IS NULL AND whatsapp_sent_at IS NULL;

ALTER TABLE public.whatsapp_accounts
  ADD COLUMN alert_destination_type text
    CHECK (alert_destination_type IN ('individual', 'group')),
  ADD COLUMN alert_destination_id   text;
```

### Cron Route — send-alerts (`apps/web/src/app/api/cron/send-alerts/route.ts`)

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createSupabaseServiceClient } from '@/lib/supabase/server'

export async function GET(req: NextRequest) {
  if (req.headers.get('Authorization') !== `Bearer ${process.env.CRON_SECRET}`) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  const supabase = createSupabaseServiceClient()

  // 1. Alertas ativos não enviados — join com ad_accounts é válido (FK real
  //    alerts.ad_account_id → ad_accounts.id). NÃO embutir whatsapp_accounts:
  //    não existe FK entre alerts e whatsapp_accounts, o embed falharia.
  const { data: alerts } = await supabase
    .from('alerts')
    .select(`
      id, workspace_id, ad_account_id, alert_type, threshold_days, projected_days,
      ad_accounts!inner(name)
    `)
    .is('resolved_at', null)
    .is('whatsapp_sent_at', null)

  if (!alerts?.length) return NextResponse.json({ processed: 0 })

  // 2. Contas WhatsApp conectadas com destino de alerta configurado, indexadas
  //    por workspace_id — cruzamento feito no código, não no PostgREST.
  const workspaceIds = [...new Set(alerts.map((a) => a.workspace_id))]
  const { data: waAccounts } = await supabase
    .from('whatsapp_accounts')
    .select('workspace_id, account_id, alert_destination_type, alert_destination_id')
    .in('workspace_id', workspaceIds)
    .eq('status', 'connected')
    .not('alert_destination_id', 'is', null)

  const waByWorkspace = new Map<string, { account_id: string; alert_destination_id: string }>()
  for (const wa of waAccounts ?? []) {
    if (!waByWorkspace.has(wa.workspace_id)) waByWorkspace.set(wa.workspace_id, wa)
  }

  let sent = 0, skipped = 0, failed = 0

  for (const alert of alerts) {
    const wa = waByWorkspace.get(alert.workspace_id)
    const adAccount = Array.isArray(alert.ad_accounts) ? alert.ad_accounts[0] : alert.ad_accounts
    if (!wa) { skipped++; continue }  // sem conta conectada ou sem destino configurado (AC 3.6.5)

    // 3. Claim atômico — tenta marcar como enviado antes de chamar o worker
    const { data: claimed } = await supabase
      .from('alerts')
      .update({ whatsapp_sent_at: new Date().toISOString(), whatsapp_destination_id: wa.alert_destination_id })
      .eq('id', alert.id)
      .is('whatsapp_sent_at', null)
      .select('id')
      .single()

    if (!claimed) { skipped++; continue }  // já enviado por outro processo

    // 4. Montar mensagem
    const projectedDate = new Date()
    projectedDate.setDate(projectedDate.getDate() + Math.round(alert.projected_days))
    const dataFormatada = projectedDate.toLocaleDateString('pt-BR')

    const text = [
      `⚠️ Alerta de Saldo — ${adAccount.name}`,
      `Saldo projetado para esgotar em ${Math.round(alert.projected_days)} dias.`,
      `Limite configurado: ${alert.threshold_days} dias de veiculação.`,
      `Ação sugerida: recarregue o saldo da conta antes de ${dataFormatada}.`,
    ].join('\n')

    // 5. Enviar via worker
    const workerRes = await fetch(`${process.env.WHATSAPP_WORKER_URL}/send`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        workspace_id: alert.workspace_id,
        account_id: wa.account_id,
        to: wa.alert_destination_id,
        text,
      }),
    }).catch(() => null)

    if (workerRes?.ok) {
      // whatsapp_sent_at já está setado pelo claim — limpar eventual last_error
      await supabase.from('alerts').update({ whatsapp_last_error: null }).eq('id', alert.id)
      sent++
    } else {
      // 6. Rollback do claim — alerta voltará a ser tentado no próximo cron
      const err = workerRes ? (await workerRes.text().catch(() => 'erro desconhecido')) : 'worker inacessível'
      await supabase
        .from('alerts')
        .update({ whatsapp_sent_at: null, whatsapp_last_error: err })
        .eq('id', alert.id)
      failed++
    }
  }

  return NextResponse.json({ processed: alerts.length, sent, skipped, failed })
}
```

**Garantia de dedup:** O UPDATE com `.is('whatsapp_sent_at', null)` é atômico no Postgres. Apenas um dos processos concorrentes receberá um registro de volta em `.select('id').single()` — o outro não terá RETURNING e será descartado como `skipped`.

**Crash após claim antes de rollback:** Se o cron morrer após `whatsapp_sent_at` ser setado (claim) mas antes do worker call ou do rollback, o alerta ficará com `whatsapp_sent_at IS NOT NULL` sem ter sido realmente enviado. Esse edge case é aceito — preferência pelo no-dup sobre guaranteed delivery, conforme requisito.

### Server Action — saveAlertDestination

```typescript
'use server'
import { revalidatePath } from 'next/cache'

export async function saveAlertDestination(
  accountId: string,
  clientId: string,
  data: { destination_type: 'individual' | 'group'; destination_id: string }
) {
  // Validação server-side de formato (antes de auth, espelha 3.3)
  if (data.destination_type === 'individual') {
    const normalized = data.destination_id.replace(/[+\s]/g, '')
    if (!/^\d{10,15}$/.test(normalized)) {
      return { error: 'Número individual inválido — use formato E.164 (ex: 5511999998888)' }
    }
  } else {
    if (!/^\d+@g\.us$/.test(data.destination_id)) {
      return { error: 'JID de grupo inválido — use formato XXXXXXXXXX@g.us' }
    }
  }

  // Auth + workspace membership
  // UPDATE whatsapp_accounts SET alert_destination_type, alert_destination_id
  //   WHERE id = accountId AND workspace_id = membership.workspace_id
  // revalidatePath('/clients/${clientId}/configuracoes')
}
```

### vercel.json — adição (apps/web/vercel.json)

Após Story 3.5 criar o arquivo, adicionar a entrada de `send-alerts`:

```json
{
  "crons": [
    {
      "path": "/api/cron/send-reports",
      "schedule": "0 * * * *"
    },
    {
      "path": "/api/cron/send-alerts",
      "schedule": "*/15 * * * *"
    }
  ]
}
```

### Relação com Story 2.9

A Story 2.9 garante dedup na **criação** do alerta (índice único parcial: 1 alerta ativo por `ad_account_id + alert_type`). A Story 3.6 garante dedup no **envio** via WhatsApp (claim atômico em `whatsapp_sent_at`). As duas camadas são complementares e independentes.

### Acesso a `ad_accounts.name` no cron

O JOIN `ad_accounts!inner(name)` requer que a `alert` tenha um `ad_account_id` válido (garantido pelo FK). O cron usa service role — ignora RLS. O campo `name` é necessário para montar a mensagem (AC 3.6.8).

---

## Testing

- **Unitário:** `saveAlertDestination` (validação individual e grupo pré-auth; IDOR; UPDATE correto); cron handler (claim ok → worker ok → sent; claim ok → worker fail → rollback; claim miss → skip; sem destino → skip)
- **Manual:** configurar destino de alerta → criar alerta via Story 2.9 → aguardar cron → verificar mensagem recebida; verificar que segundo run do cron ignora o alerta já enviado (`whatsapp_sent_at IS NOT NULL`)

---

## Dev Agent Record

*(preenchido pelo @dev durante implementação)*

**Agent:** @dev (Dex)
**Modo:** Interativo (checkpoints T3, T5, T6 cumpridos)

### Decisões autônomas (IDS)

- **[AUTO-DECISION]** T1 confirmado sem retrabalho (reason: fix group JID já aplicado pela Story 3.5 T1 nesta mesma sessão — `to.includes('@')` presente em `apps/whatsapp-worker/src/index.ts`).
- **[AUTO-DECISION]** `saveAlertDestination` recebe `clientId` como 2º parâmetro (reason: necessário para `revalidatePath('/clients/${clientId}/configuracoes')`; a assinatura do AC 3.6.7 especifica só `accountId` + data, mas o caminho de revalidação exige o clientId — mesmo padrão das actions da 3.3/3.5).
- **[AUTO-DECISION]** Página busca a 1ª `whatsapp_accounts` do workspace por `created_at` ASC, qualquer status (reason: o destino de alerta deve ser configurável mesmo com a conta temporariamente desconectada; o cron é quem filtra `status = 'connected'` na hora do envio).
- **[AUTO-DECISION]** `AlertDestinationForm` com `accountId null` renderiza hint "Conecte uma conta..." em vez de esconder a seção (reason: descobribilidade — o gestor entende o pré-requisito).
- **[AUTO-DECISION]** `projected_days` convertido com `Math.round(Number(...))` (reason: coluna é `numeric` — chega como string/decimal do PostgREST; mensagem exibe dias inteiros).
- **[AUTO-DECISION]** Try/catch em torno do fetch do worker com `workerError` default `'worker inacessível'` (reason: fetch pode lançar (rede) além de responder não-ok; ambos os caminhos fazem rollback do claim — AC 3.6.3).
- **[AUTO-DECISION]** `export const dynamic = 'force-dynamic'` na cron route (reason: idem 3.5 — rota autenticada por header, nunca cacheável).

### File List

**Criados:**
- `supabase/migrations/20260101000016_alerts_whatsapp.sql`
- `apps/web/src/app/actions/alert-destination.ts`
- `apps/web/src/app/api/cron/send-alerts/route.ts`
- `apps/web/src/components/molecules/AlertDestinationForm.tsx`
- `apps/web/src/__tests__/alert-destination.test.ts`

**Modificados:**
- `apps/web/src/app/(dashboard)/clients/[id]/configuracoes/page.tsx` (query whatsapp_accounts + sub-seção "Destino de Alertas" na seção WhatsApp)
- `apps/web/vercel.json` (cron send-alerts `*/15 * * * *`)

### Validação

- `pnpm --filter @advezo/web exec vitest run alert-destination` → **12/12 PASS**
- `pnpm --filter @advezo/web exec tsc --noEmit` → PASS
- `pnpm --filter @advezo/web lint` → 0 errors (warnings pré-existentes fora do escopo)
- Suíte completa: 321 passed, 1 failed — falha pré-existente documentada (`workspace-actions.test.ts`)
- `supabase migration list` → Local=Remote até 000016
- Testes cobrem os 4 cenários críticos de dedup: claim ok + worker ok → sent; claim ok + worker fail → rollback + last_error; claim perdido → skip sem worker call; sem destino → skip silencioso sem claim

---

## QA Results

*(preenchido pelo @qa após implementação)*

---

## Change Log

| Data | Autor | Ação |
|------|-------|------|
| 2026-07-06 | Morgan (@pm) | Story criada a partir do PRD (AC 3.6.1–3.6.10). Wave 4, paralela à Story 3.5. Migration 000016 reservada. Status: Ready. |
| 2026-07-07 | Morgan (@pm) | Revisão pré-dev: cron `send-alerts` corrigido — embed `whatsapp_accounts!inner(...)` removido (não há FK entre `alerts` e `whatsapp_accounts`). Join com `ad_accounts` mantido (FK real). `whatsapp_accounts` buscada em consulta separada por `workspace_id` + `status = 'connected'` + destino configurado, cruzada no código via `Map`. |
| 2026-07-07 | Dex (@dev) | Implementação completa T1–T10 (interativo, checkpoints T3/T5/T6 aprovados). Migration 000016 aplicada (Local=Remote); claim atômico UPDATE WHERE whatsapp_sent_at IS NULL + rollback em falha; `saveAlertDestination` com validação pré-auth; UI Destino de Alertas na seção WhatsApp; vercel.json cron 15min. 12 testes PASS, typecheck+lint limpos, zero regressões. Status: Ready → InProgress → InReview. |
