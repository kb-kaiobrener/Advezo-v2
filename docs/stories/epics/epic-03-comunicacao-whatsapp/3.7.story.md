# Story 3.7 — Dashboard Compartilhável com Branding da Agência

## Status

**Status:** Ready
**Epic:** 03 — Comunicação com Cliente via WhatsApp
**Wave:** 3 — Configuração de relatórios + Dashboard compartilhável
**Depende de:** Story 3.1 Done (worker estável); sem dependência de Story 3.3 ou 3.2
**Criada em:** 2026-07-04
**Modo:** YOLO (SSR puro, sem envio WhatsApp, sem side-effects de infra)

## Executor Assignment

| Role | Agent | Notes |
|------|-------|-------|
| Executor | @dev (Dex) | SSR + Storage + JWT-free token — modo YOLO |
| Quality Gate | @qa (Quinn) | YOLO — foco em segurança (service key nunca exposta), RLS, performance |

---

## Story

**As a** gestor de tráfego,
**I want to** gerar um link público de dashboard para cada cliente com meu logo e as métricas que eu selecionar,
**so that** eu substitua o envio de planilha ou print e o cliente possa ver os dados em tempo real sem precisar de login.

---

## Acceptance Criteria

- [ ] **AC 3.7.1** — UI em Clientes → [cliente] → Configurações: seção "Dashboard Compartilhável" com formulário contendo: seleção de métricas (multi-checkbox: Investimento, Impressões, Cliques, CTR, Conversões, Receita, ROAS, CPL), upload de logo (PNG/JPEG ≤ 2MB), proteção por senha (campo opcional — em branco = dashboard público). Botão "Gerar link" cria ou atualiza a config sem gerar novo token se já existe.

- [ ] **AC 3.7.2** — Link público no formato `/dashboard/[token]` onde `token` é um hex de 40 caracteres gerado server-side (`crypto.randomBytes(20).toString('hex')`). Token gerado uma única vez por cliente — reconfigurar métricas ou logo não muda o token. Server Action `saveDashboardConfig` faz upsert em `dashboard_configs` com conflito `workspace_id, client_id`.

- [ ] **AC 3.7.3** — Rota pública `/dashboard/[token]` implementada como Next.js Server Component. Fluxo server-side: (1) query `dashboard_configs` por token usando `createSupabaseServiceClient()` → (2) valida `is_active = true` → (3) se `password_hash != null`, redireciona para tela de senha → (4) agrega métricas do mês atual em `campaign_metrics` → (5) renderiza HTML. `SUPABASE_SERVICE_ROLE_KEY` nunca é enviada ao browser — conteúdo gerado exclusivamente no servidor (NFR-2).

- [ ] **AC 3.7.4** — Dashboard exibe: logo da agência (se configurado), nome do cliente, período exibido (mês atual), métricas selecionadas com valores formatados em PT-BR, data e hora da última atualização (campo `synced_at` de `campaign_metrics`).

- [ ] **AC 3.7.5** — Proteção por senha: quando `password_hash` configurado, rota pública exibe formulário de senha antes dos dados. Validação via POST para `/api/dashboard/[token]/auth` (Route Handler). Validação correta → seta cookie httpOnly `dash_auth_[token]` com valor HMAC-SHA256(token + password_hash + secret) — expira em 24h. Próximas visitas com cookie válido pulam a tela de senha sem nova validação de DB.

- [ ] **AC 3.7.6** — Link copiável na UI do gestor (botão "Copiar link" que executa `navigator.clipboard.writeText`). Botão "Desativar link" chama Server Action `deactivateDashboard(clientId)` que seta `is_active = false`. Dashboard desativado exibe página 404 (não retorna dados). Reativar com `saveDashboardConfig` gera nova config com **mesmo token** (não gera novo).

- [ ] **AC 3.7.7** — Upload de logo para Supabase Storage no bucket `dashboard-logos` (público, sem RLS — acesso via URL pública). Chave: `{workspaceId}/{clientId}/logo.{ext}`. Tamanho máximo 2MB validado no Server Action antes do upload. Logo URL armazenada em `dashboard_configs.logo_url`.

- [ ] **AC 3.7.8** — **Tempo de carregamento da rota pública abaixo de 2 segundos em 4G** (Lighthouse Performance Score ≥ 90, medido em produção Vercel). Requisitos técnicos: Server Component com `export const revalidate = 300` (cache 5 min no edge), sem JS desnecessário no bundle do cliente, `<Image>` do Next.js para o logo (otimização automática WebP + CDN), nenhum fetch client-side na rota pública, CSS inline via Tailwind (sem folha de estilo externa).

---

## Tasks

- [ ] **T1** — Criar migration `supabase/migrations/20260101000014_dashboard_configs.sql`: tabela `dashboard_configs` + bucket `dashboard-logos` (ver Dev Notes)
- [ ] **T2** — Aplicar migration: `supabase db push` e confirmar `supabase migration list` (Local=Remote)
- [ ] **T3** — Implementar Server Actions em `apps/web/src/app/actions/dashboard.ts`: `saveDashboardConfig`, `uploadDashboardLogo`, `deactivateDashboard`
- [ ] **T4** — UI: componente `DashboardConfigForm` em `apps/web/src/components/molecules/DashboardConfigForm.tsx` — formulário com multi-checkbox de métricas, upload de logo, campo de senha, botão copiar link
- [ ] **T5** — Rota pública `apps/web/src/app/dashboard/[token]/page.tsx` (Server Component puro, `export const revalidate = 300`, zero chamada a `cookies()`) — validação do token via DB, fetch de métricas, renderização
- [ ] **T5.5** — Corrigir `apps/web/src/proxy.ts`: adicionar `isPublicDashboard` e excluí-lo de `isDashboard` para que `/dashboard/*` (link público) não seja redirecionado para `/login` pelo guard de auth existente. Em seguida, adicionar a lógica do guard de senha: para rotas `/dashboard/:token` (excluindo `/dashboard/:token/senha`), verificar `password_hash` via Supabase REST + validar cookie HMAC com WebCrypto; redirecionar para `/dashboard/:token/senha` se autenticação necessária e ausente/inválida (ver Dev Notes — correção exata em `proxy.ts`)
- [ ] **T6** — Rota de senha `apps/web/src/app/dashboard/[token]/senha/page.tsx` — Client Component com form de senha, POST para `/api/dashboard/[token]/auth`, redireciona para `/dashboard/[token]` após auth bem-sucedida
- [ ] **T7** — Route Handler `apps/web/src/app/api/dashboard/[token]/auth/route.ts` — valida senha, seta cookie httpOnly
- [ ] **T8** — Integrar seção "Dashboard Compartilhável" na página `/clients/[id]/configuracoes` (Server Component busca config existente e passa como prop)
- [ ] **T9** — Criar bucket `dashboard-logos` no Supabase Storage (público) via migration ou script manual documentado no Dev Agent Record
- [ ] **T10** — Testes unitários das Server Actions: `saveDashboardConfig` (upsert, token gerado uma vez, auth), `deactivateDashboard` (is_active=false), `uploadDashboardLogo` (validação de tamanho)
- [ ] **T11** — Atualizar story: checkboxes, Dev Agent Record (File List), Change Log

---

## Dev Notes

### Migration 000014 — dashboard_configs

```sql
CREATE TABLE public.dashboard_configs (
  id               uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
  workspace_id     uuid        NOT NULL REFERENCES public.workspaces(id)  ON DELETE CASCADE,
  client_id        uuid        NOT NULL REFERENCES public.clients(id)     ON DELETE CASCADE,
  token            text        NOT NULL UNIQUE
                               DEFAULT encode(gen_random_bytes(20), 'hex'),
  logo_url         text,       -- URL pública do Supabase Storage, ou NULL
  selected_metrics text[]      NOT NULL DEFAULT ARRAY['spend','impressions','clicks'],
  password_hash    text,       -- SHA-256 HMAC (ver seção Senha abaixo), NULL = público
  password_salt    text,       -- salt aleatório usado no hash
  is_active        boolean     NOT NULL DEFAULT true,
  created_at       timestamptz NOT NULL DEFAULT now(),
  updated_at       timestamptz NOT NULL DEFAULT now(),
  UNIQUE (workspace_id, client_id)
);

ALTER TABLE public.dashboard_configs ENABLE ROW LEVEL SECURITY;

CREATE POLICY workspace_isolation ON public.dashboard_configs
  FOR ALL USING (workspace_id = auth_workspace_id());
-- NOTA: rota pública /dashboard/[token] usa createSupabaseServiceClient() server-side
-- e não passa pelo RLS — o service client tem acesso irrestrito por design.

CREATE TRIGGER dashboard_configs_set_updated_at
  BEFORE UPDATE ON public.dashboard_configs
  FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();

GRANT SELECT, INSERT, UPDATE, DELETE ON public.dashboard_configs TO service_role;
GRANT SELECT, INSERT, UPDATE, DELETE ON public.dashboard_configs TO authenticated;
```

### Métricas disponíveis

```typescript
export const AVAILABLE_METRICS = [
  { key: 'spend',       label: 'Investimento',  calculated: false },
  { key: 'impressions', label: 'Impressões',     calculated: false },
  { key: 'clicks',      label: 'Cliques',        calculated: false },
  { key: 'ctr',         label: 'CTR',            calculated: true  }, // clicks/impressions
  { key: 'conversions', label: 'Conversões',     calculated: false },
  { key: 'revenue',     label: 'Receita',        calculated: false },
  { key: 'roas',        label: 'ROAS',           calculated: true  }, // revenue/spend
  { key: 'cpl',         label: 'CPL',            calculated: true  }, // spend/conversions
] as const
```

### Server Actions (`apps/web/src/app/actions/dashboard.ts`)

```typescript
'use server'
import { revalidatePath } from 'next/cache'
import crypto from 'node:crypto'

export async function saveDashboardConfig(clientId: string, data: {
  selected_metrics: string[]
  password?: string | null   // em branco ou null = sem senha
}) {
  // 1. getWorkspaceMembership()
  // 2. busca config existente por (workspace_id, client_id)
  //    - se existe: mantém o token existente (não gera novo)
  //    - se não existe: token gerado pelo DEFAULT da migration (gen_random_bytes)
  // 3. se password fornecida:
  //    const salt = crypto.randomBytes(8).toString('hex')
  //    const hash = crypto.createHmac('sha256', process.env.DASHBOARD_AUTH_SECRET!)
  //                       .update(data.password + salt).digest('hex')
  //    salvar password_hash = hash, password_salt = salt
  // 4. se password = null/vazio: limpar password_hash e password_salt
  // 5. upsert com onConflict: 'workspace_id,client_id'
  // 6. revalidatePath(`/clients/${clientId}/configuracoes`)
  return { success: true, token: '...' }
}

export async function deactivateDashboard(clientId: string) {
  // update is_active = false
  // revalidatePath(`/clients/${clientId}/configuracoes`)
}

export async function uploadDashboardLogo(clientId: string, formData: FormData) {
  // 1. getWorkspaceMembership()
  // 2. extrair File do formData
  // 3. validar size <= 2MB (2 * 1024 * 1024 bytes)
  // 4. validar mime: image/png ou image/jpeg
  // 5. upload para Storage: `${workspaceId}/${clientId}/logo.${ext}`
  //    no bucket 'dashboard-logos' (público)
  //    usar createSupabaseServiceClient().storage.from('dashboard-logos').upload(path, buffer, { upsert: true })
  // 6. obter URL pública: supabase.storage.from('dashboard-logos').getPublicUrl(path)
  // 7. update dashboard_configs.logo_url = publicUrl
  // 8. revalidatePath(`/clients/${clientId}/configuracoes`)
}
```

### Arquitetura de cache e autenticação — decisão fechada

#### Por que `cookies()` no `page.tsx` não resolve

No App Router do Next.js sem PPR, chamar `cookies()` (de `next/headers`) em qualquer ponto do Server Component — mesmo dentro de um `if` — opta a **rota inteira** para dynamic rendering. A decisão é por rota, não por branch de código. Isso significa que um `if (config.password_hash) { cookies()... }` tornaria `/dashboard/[token]` dinâmica para todos os visitantes, inclusive dashboards sem senha, destruindo o ISR.

#### Arquitetura correta: middleware como gate

```
Visitante → GET /dashboard/:token
              ↓
         Middleware (Edge)
              ├── lê password_hash via Supabase REST (fetch leve)
              ├── se sem senha → NextResponse.next() (page.tsx serve ISR)
              └── se com senha:
                    ├── cookie válido → NextResponse.next()
                    └── sem cookie / inválido → redirect /dashboard/:token/senha

/dashboard/[token]/page.tsx     ← ISR puro, export const revalidate = 300, zero cookies()
/dashboard/[token]/senha/page.tsx ← PasswordGate, pode ser dinâmica, redireciona após auth
```

#### `page.tsx` — ISR real, zero `cookies()`

```typescript
// apps/web/src/app/dashboard/[token]/page.tsx
export const revalidate = 300  // ISR 5 min — funciona porque middleware já fez o gate

import { notFound } from 'next/navigation'
import { createSupabaseServiceClient } from '@advezo/database'

export default async function DashboardPage({ params }: { params: { token: string } }) {
  const db = createSupabaseServiceClient()  // NUNCA exposta ao browser

  const { data: config } = await db
    .from('dashboard_configs')
    .select('*, clients(name)')
    .eq('token', params.token)
    .eq('is_active', true)
    .single()

  if (!config) notFound()

  // Sem cookies() aqui — o middleware já garantiu que quem chegou até este ponto
  // está autorizado (seja por não ter senha, seja por ter cookie válido).

  // Busca métricas do mês atual
  const now = new Date()
  const start = new Date(now.getFullYear(), now.getMonth(), 1).toISOString().split('T')[0]
  const end   = new Date(now.getFullYear(), now.getMonth() + 1, 0).toISOString().split('T')[0]
  // ...

  return <DashboardView config={config} metrics={...} />
}
```

#### Middleware — proteção por senha (T5.5)

O middleware deve interceptar `/dashboard/:token` (sem o sufixo `/senha` para evitar loop) e:
1. Fazer fetch Supabase REST para buscar `password_hash` e `password_salt` — query leve, um campo
2. Se `password_hash = null` → `NextResponse.next()` (dashboard público)
3. Se `password_hash != null` → verificar cookie; se inválido → redirect `/senha`

**WebCrypto no Edge:** middleware roda no Edge runtime, sem acesso a `node:crypto`. Usar a WebCrypto API do browser (disponível em Edge Functions do Next.js via `globalThis.crypto`):

```typescript
// Verificação de cookie com WebCrypto (sem node:crypto)
async function verifyDashboardCookie(
  cookieValue: string,
  token: string,
  passwordHash: string,
  secret: string
): Promise<boolean> {
  const enc = new TextEncoder()
  const key = await crypto.subtle.importKey(
    'raw',
    enc.encode(secret),
    { name: 'HMAC', hash: 'SHA-256' },
    false,
    ['verify']
  )
  // Converter o valor hex do cookie de volta para bytes para usar crypto.subtle.verify
  const cookieBytes = Uint8Array.from(
    cookieValue.match(/../g)!.map(h => parseInt(h, 16))
  )
  const message = enc.encode(token + passwordHash)
  // crypto.subtle.verify é timing-safe por spec
  return crypto.subtle.verify('HMAC', key, cookieBytes, message)
}
```

**Correção obrigatória em `apps/web/src/proxy.ts` — fazer ANTES de qualquer outra mudança no middleware:**

`isDashboard` hoje é `true` para qualquer rota que não seja login/register/auth/onboarding/raiz — incluindo `/dashboard/[token]`. Isso faz o guard de auth redirecionar visitantes sem sessão para `/login` antes mesmo do guard de senha do T5.5 executar, quebrando a funcionalidade inteira.

Aplicar a seguinte alteração cirúrgica:

```diff
- const isDashboard = !isAuthRoute && !isOnboarding && pathname !== '/'
+ const isPublicDashboard = pathname.startsWith('/dashboard/')
+ const isDashboard = !isAuthRoute && !isOnboarding && !isPublicDashboard && pathname !== '/'
```

Isso preserva a proteção de login em `/dashboard` (rota interna do app autenticado) e libera `/dashboard/*` (links públicos do cliente) para chegar ao guard de senha.

**Lógica do guard de senha (adicionar após a correção acima):**

```typescript
// Guard de senha para dashboards públicos — executar ANTES do guard de auth
if (isPublicDashboard) {
  return handleDashboardPasswordGate(req)  // função separada (ver WebCrypto abaixo)
}
```

**Matcher e integração com auth existente:**

```typescript
// handleDashboardPasswordGate intercepta /dashboard/:token
// mas NÃO /dashboard/:token/senha (evita redirect loop)
// e NÃO /api/dashboard/:token/auth (Route Handler de validação)
const isSenhaRoute = pathname.match(/^\/dashboard\/[^/]+\/senha/)
const isAuthEndpoint = pathname.startsWith('/api/dashboard/')
if (isSenhaRoute || isAuthEndpoint) return NextResponse.next()
```

**Custo por requisição:** um fetch REST ao Supabase por requisição a `/dashboard/:token`. Aceitável — é uma query `SELECT password_hash WHERE token = :token` com índice UNIQUE, sub-millisegundo no banco. Alternativa sem fetch: encoded no próprio token (complexidade não justificada para este volume).

**Por que não há risco de exposição da service key:**
- `SUPABASE_SERVICE_ROLE_KEY` usada no fetch do middleware (Edge, servidor) e em Server Components
- Nunca exposta via `NEXT_PUBLIC_` — não vai para o bundle do cliente

**Variável `DASHBOARD_AUTH_SECRET`:** segredo dedicado para HMAC de senhas e cookies. Nunca reutilizar `SUPABASE_SERVICE_ROLE_KEY`. Gerar com:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```
Adicionar ao `.env.local` e ao painel de variáveis do Vercel e Railway.

### Proteção por senha — Route Handler

```typescript
// apps/web/src/app/api/dashboard/[token]/auth/route.ts
import { NextRequest, NextResponse } from 'next/server'
import crypto from 'node:crypto'

export async function POST(req: NextRequest, { params }: { params: { token: string } }) {
  const { password } = await req.json() as { password?: string }
  if (!password) return NextResponse.json({ error: 'senha obrigatória' }, { status: 400 })

  const db = createSupabaseServiceClient()
  const { data: config } = await db
    .from('dashboard_configs')
    .select('password_hash, password_salt')
    .eq('token', params.token)
    .eq('is_active', true)
    .single()

  if (!config?.password_hash) return NextResponse.json({ error: 'token inválido' }, { status: 404 })

  const secret = process.env.DASHBOARD_AUTH_SECRET!
  const expected = crypto.createHmac('sha256', secret)
    .update(password + config.password_salt)
    .digest('hex')

  // timingSafeEqual previne timing attacks — NUNCA comparar hashes com !==
  const hashesMatch = crypto.timingSafeEqual(
    Buffer.from(expected),
    Buffer.from(config.password_hash)
  )
  if (!hashesMatch) {
    return NextResponse.json({ error: 'senha incorreta' }, { status: 401 })
  }

  // Cookie de auth — httpOnly, secure, SameSite=Lax, 24h
  const cookieVal = crypto.createHmac('sha256', secret)
    .update(params.token + config.password_hash)
    .digest('hex')

  const res = NextResponse.json({ ok: true })
  res.cookies.set(`dash_auth_${params.token}`, cookieVal, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: 60 * 60 * 24,
    path: `/dashboard/${params.token}`,
  })
  return res
}
```

### Performance < 2s em 4G (AC 3.7.8)

Requisitos obrigatórios para atingir o AC:

| Requisito | Implementação |
|-----------|--------------|
| Cache de 5 min no edge | `export const revalidate = 300` em `page.tsx` — funciona porque `cookies()` foi removido da rota |
| Middleware leve | Fetch REST Supabase no middleware (~5ms) — não afeta TTFB do ISR cacheado |
| Sem JS no bundle do cliente | `page.tsx` é Server Component puro; `senha/page.tsx` é rota separada |
| Logo otimizado | `<Image src={logo_url} ...>` do Next.js (WebP automático + CDN Vercel) |
| CSS inline | Tailwind (sem folha externa); sem Google Fonts na rota pública |
| Sem fetch client-side | Todos os dados carregados no Server Component |
| Agregação server-side | Uma query SUM agrupada em `campaign_metrics`, não N queries individuais |

Query de agregação do mês atual:
```typescript
const start = new Date(now.getFullYear(), now.getMonth(), 1).toISOString().split('T')[0]
const end   = new Date(now.getFullYear(), now.getMonth() + 1, 0).toISOString().split('T')[0]

const { data: metrics } = await db
  .from('campaign_metrics')
  .select('spend, impressions, clicks, conversions, revenue')
  .in('campaign_id', campaignIds)
  .gte('date', start)
  .lte('date', end)
// aggregateMetrics(metrics) da Story 3.4 — já exportada
```

### Bucket Supabase Storage — `dashboard-logos`

O bucket precisa existir antes do primeiro upload. Opções:
1. Criar via `supabase.storage.createBucket('dashboard-logos', { public: true })` em um script de seed
2. Criar manualmente no Supabase Dashboard → Storage → New Bucket → `dashboard-logos` → Public = true
3. Criar via migration SQL usando `storage.buckets` (se suportado pela versão)

**@dev deve documentar no Dev Agent Record qual abordagem foi utilizada.**

### Integração na página de configurações

```typescript
// Server Component configuracoes/page.tsx
const { data: dashboardConfig } = await serviceClient
  .from('dashboard_configs')
  .select('*')
  .eq('client_id', clientId)
  .eq('workspace_id', workspaceId)
  .maybeSingle()
```

Passado como prop para `<DashboardConfigForm config={dashboardConfig} clientId={id} />`.

---

## Testing

- **Unitário:** `saveDashboardConfig` (upsert, token não muda em re-save, auth, workspace), `deactivateDashboard` (is_active=false), `uploadDashboardLogo` (rejeita >2MB, rejeita non-image)
- **Manual (segurança e cache):** (1) DevTools → Network → confirmar que nenhuma resposta de `/dashboard/[token]` contém a service key ou variável de ambiente; (2) dashboard desativado → 404; (3) dashboard com senha → middleware redireciona para `/senha` → formulário → cookie setado → redirect para `/dashboard/[token]` → dados exibidos; (4) senha errada → 401; (5) confirmar cabeçalho `x-nextjs-cache: HIT` em segunda visita a dashboard público (ISR funcionando)
- **Performance:** Deploy em Vercel → Lighthouse (mobile, 4G simulado) → Performance Score ≥ 90 e LCP < 2s

---

## Dev Agent Record

*(preenchido pelo @dev durante implementação)*

**Agent:** @dev (Dex)
**Modo:** YOLO

### File List

**Criados:**
*(a preencher)*

**Modificados:**
*(a preencher)*

---

## Change Log

| Data | Autor | Ação |
|------|-------|------|
| 2026-07-04 | Morgan (@pm) | Story criada a partir do PRD (AC 3.7.1–3.7.8). Restrição de segurança NFR-2 (service key nunca exposta) e AC de performance (< 2s em 4G) detalhadas em Dev Notes. Migration 000014 reservada. Status: Ready. |
| 2026-07-04 | Morgan (@pm) | Revisão pré-dev r1: (1) Contradição `force-static` vs cookie resolvida — regra de cache documentada por caso (público: ISR revalidate 300; protegido: force-dynamic via cookies()); (2) Segredo do HMAC trocado de `SUPABASE_SERVICE_ROLE_KEY` para `DASHBOARD_AUTH_SECRET` dedicada; (3) Comparação de hash trocada por `crypto.timingSafeEqual` para prevenir timing attacks. |
| 2026-07-04 | Morgan (@pm) | Revisão pré-dev r2: Correção fundamental da arquitetura de cache. `cookies()` no `page.tsx` (mesmo dentro de `if`) opta a rota inteira para dynamic — sem PPR, isso destrói o ISR para todos os dashboards. Solução: guard movido para o middleware (Edge). `page.tsx` fica livre de `cookies()`, usa `export const revalidate = 300` de verdade. Middleware faz fetch REST Supabase para checar `password_hash` + verifica cookie com WebCrypto; redireciona para `/dashboard/:token/senha` se não autenticado. T5.5 adicionado (middleware); T6 alterado para `senha/page.tsx`; tabela de performance atualizada. |
| 2026-07-06 | Morgan (@pm) | Revisão pré-dev r3: Correção de proxy.ts — `isDashboard` incluía `/dashboard/*` (rota pública), redirecionando visitantes sem login para /login antes do guard de senha executar. Adicionado `isPublicDashboard = pathname.startsWith('/dashboard/')` e excluído de `isDashboard`. Diff exato e ordem de execução dos guards documentados em Dev Notes. T5.5 atualizado para referenciar proxy.ts explicitamente. |
