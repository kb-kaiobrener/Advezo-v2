# Story 1.4 — Gestão de Clientes (CRUD)

## Status

**Status:** Done
**Epic:** 01 — Fundação e Navegação
**Wave:** 3 (sequencial — deve completar antes de 1.5)
**Depende de:** Story 1.1 (infra), 1.2 (auth + workspace), 1.3 (design system)
**Criada em:** 2026-06-25

## Executor Assignment

| Role | Agent | Notes |
|------|-------|-------|
| Executor | @dev (Dex) | CRUD de clientes |
| Quality Gate | @architect (Aria) | Validação de RLS e isolamento por workspace |

---

## Story

**As a** gestor de tráfego,
**I want to** cadastrar, visualizar, editar e remover meus clientes (agências/empresas),
**so that** eu possa organizar as contas de mídia de cada cliente dentro do meu workspace de forma isolada.

---

## Acceptance Criteria

- [x] **AC 1.4.1** — Tabela `clients` existe no banco com os campos: `id`, `workspace_id`, `name`, `document` (CNPJ/CPF — opcional), `contact_email` (opcional), `contact_phone` (opcional), `deleted_at` (soft delete), `created_at`, `updated_at`. Schema exato em `docs/architecture.md` Section 9. Sem campos `logo_url` ou `status`.
- [x] **AC 1.4.2** — RLS em `clients`: usuário só acessa clients do próprio workspace. Policy `workspace_isolation` via `auth_workspace_id()` (padrão arquitetural — migration 000003).
- [x] **AC 1.4.3** — Página de listagem em `apps/web/src/app/(dashboard)/clients/page.tsx`: mostra lista de clientes ativos (`WHERE deleted_at IS NULL`) com inicial do nome como avatar, nome, `contact_email` (se presente), e botão "+ Novo Cliente". Server Component com fetch server-side.
- [x] **AC 1.4.4** — Formulário de criação/edição em `/(dashboard)/clients/new/page.tsx` e `/(dashboard)/clients/[id]/edit/page.tsx`: campos `name` (obrigatório), `document` (opcional), `contact_email` (opcional), `contact_phone` (opcional). `ClientForm.tsx` com react-hook-form + zod.
- [x] **AC 1.4.5** — Server Actions para mutações: `createClient`, `updateClient`, `archiveClient` em `apps/web/src/app/actions/clients.ts`. Cada action revalida o cache via `revalidatePath('/clients')`.
- [x] **AC 1.4.6** — Arquivar (soft delete): modal de confirmação usando ShadCN AlertDialog base-nova ("Arquivar cliente? O cliente ficará oculto e pode ser restaurado."). `archiveClient` define `deleted_at = NOW()`. Sem exclusão física.
- [x] **AC 1.4.7** — Estado vazio: quando não há clientes, mostrar `EmptyState` com `icon=Users`, `title="Nenhum cliente cadastrado"`, `subtitle="Adicione seu primeiro cliente para começar"`.
- [x] **AC 1.4.8** — Paginação simples: listagem com limit 20, botão "Carregar mais" cursor-based via `created_at` (URL param `?after=`).
- [x] **AC 1.4.9** — Testes: 8 testes — ClientSchema (5) + createClient (2) + archiveClient (1). Total 30/30 passando.
- [x] **AC 1.4.10** — `packages/types/src/index.ts` exporta interface `Client` completa com todos os campos da tabela.

---

## Tasks

- [x] **T1** — Migrações: `000002_auth_workspace_id_hook.sql` (função JWT + hook + reverta Wave 2) + `000003_clients.sql` (DDL + RLS + trigger updated_at).
- [x] **T2** — `packages/types/src/index.ts` com interface `Client` completa (já existia desde Story 1.1).
- [x] **T3** — Server Actions: `createClient`, `updateClient`, `archiveClient` em `apps/web/src/app/actions/clients.ts` com validação Zod (`ClientSchema` em `src/lib/schemas/clients.ts`).
- [x] **T4** — Página de listagem `/(dashboard)/clients/page.tsx` Server Component. Fetch server-side, lista ou EmptyState, paginação cursor-based.
- [x] **T5** — `ClientCard.tsx` (Client Component): avatar + nome + email + AlertDialog de arquivamento.
- [x] **T6** — `ClientForm.tsx` (Client Component) com react-hook-form + zod. Páginas `clients/new` e `clients/[id]/edit`.
- [x] **T7** — Soft delete via `archiveClient` Server Action + AlertDialog de confirmação na ClientCard.
- [x] **T8** — 30/30 testes passando. TypeCheck ✅ · Build ✅ (11 rotas).

---

## Dev Notes

### DDL da Tabela `clients` (docs/architecture.md Section 9)

```sql
CREATE TABLE clients (
  id            uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  workspace_id  uuid NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  name          text NOT NULL,
  document      text,
  contact_email text,
  contact_phone text,
  deleted_at    timestamptz,
  created_at    timestamptz NOT NULL DEFAULT now(),
  updated_at    timestamptz NOT NULL DEFAULT now()
);

ALTER TABLE clients ENABLE ROW LEVEL SECURITY;
CREATE POLICY workspace_isolation ON clients USING (workspace_id = auth_workspace_id());
```

Sem `logo_url`, sem `status`. Estado ativo/arquivado derivado de `deleted_at IS NULL`.

### Server Actions Pattern

```typescript
// apps/web/src/app/actions/clients.ts
'use server'
import { z } from 'zod'
import { revalidatePath } from 'next/cache'
import { createSupabaseServerClient } from '@advezo/database'

const ClientSchema = z.object({
  name:          z.string().min(1, 'Nome obrigatório'),
  document:      z.string().optional(),
  contact_email: z.string().email().optional().or(z.literal('')),
  contact_phone: z.string().optional(),
})

export async function createClient(formData: z.infer<typeof ClientSchema>) {
  const validated = ClientSchema.parse(formData)
  const supabase = createSupabaseServerClient()
  // workspace_id vem do contexto do usuário autenticado
  const { data: { user } } = await supabase.auth.getUser()
  // ... insert + revalidatePath
}
```

### Absolute Imports (Article VI — Constitution)

```typescript
import { createSupabaseServerClient } from '@advezo/database'
import { Client } from '@advezo/types'
import { EmptyState } from '@/components/molecules/EmptyState'
```

### Paginação Cursor-Based

```typescript
// Listagem server-side
const { data } = await supabase
  .from('clients')
  .select('*')
  .order('created_at', { ascending: false })
  .limit(20)
  // Para "carregar mais": .lt('created_at', cursor)
```

### Referências de UI

- Wireframe listagem: `docs/ux-design-spec.md` Section 4.3
- Empty state Clientes: `docs/ux-design-spec.md` Section 5.2
- Componentes reutilizáveis desta story: `StatusBadge`, `EmptyState` (ambos da 1.3)

---

## Testing

- **Unit:** Server Actions com Supabase mock (de `packages/mocks`)
- **Component:** ClientCard renderiza nome, contact_email (se presente), ações arquivar/editar
- **Security:** Query com workspace_id errado retorna vazio (RLS)
- **Soft delete:** `archiveClient` seta `deleted_at`; listagem com `WHERE deleted_at IS NULL` exclui arquivados
- **Form:** Validação zod rejeita nome vazio e email inválido

---

## Change Log

| Data | Autor | Ação |
|------|-------|------|
| 2026-06-25 | Morgan (@pm) | Story criada para execução do Epic 1 |
| 2026-06-25 | Morgan (@pm) | Ready — Wave 2 gate PASS; Wave 3 liberada; modo interactive; sequencial (1.4 → 1.5) |
| 2026-06-25 | Dex (@dev) | InProgress — 2 checkpoints interativos concluídos; todos os ACs implementados; 30/30 testes; build ✅ |
| 2026-06-25 | Aria (@architect) | QA Gate PASS (re-gate) — EmptyState RSC fix validado; isolamento cross-workspace testado real; gate de deploy documentado em docs/architecture.md |
| 2026-06-25 | Dex (@dev) | Fix pós-QA Gate: (1) EmptyState.tsx agora aceita `href` como alternativa a `onClick` — Server Components usam `href`, eliminando o erro RSC de função não-serializável em clients/page.tsx e design/page.tsx; (2) Adicionado teste de isolamento cross-workspace real em auth-workspace-id.test.ts (3º teste: cria cliente em WS-A e WS-B, verifica usuário A vê apenas WS-A); (3) adminClient tipado como `any` no teste de integração (padrão para cliente admin sem tipos gerados). TypeCheck ✅ · 30/30 testes · aguardando re-gate Aria. |

---

## Dev Agent Record

**Implementado por:** @dev (Dex) — modo interactive (2 checkpoints)
**Branch:** master
**Commit:** pendente (@devops após QA gate)
**Decisões interativas:**
- auth_workspace_id() + custom_access_token_hook criados (corrigindo desvio Wave 2); policies revertidas ao padrão arquitetural; teste de integração documenta falha ruidosa se hook não configurado
- react-hook-form + Server Action (conforme AC 1.4.4); sem validação CNPJ/CPF (Article IV — sem requisito real)
**Desvios do plano original:**
- Migração dividida em 000002 (hook + Wave 2 fix) + 000003 (clients) para separação de concerns
- Zod downgrade v3 (v4 incompatível com @hookform/resolvers disponível)
- asChild não suportado pelo base-ui — substituído por Link+buttonVariants e AlertDialogTrigger direto

---

## QA Results

**QA Gate:** @architect (Aria) — re-gate após 2 ciclos de fix
**Verdict:** PASS — TypeCheck ✅ · 30/30 · EmptyState RSC fix · isolamento cross-workspace testado · gate de deploy documentado
