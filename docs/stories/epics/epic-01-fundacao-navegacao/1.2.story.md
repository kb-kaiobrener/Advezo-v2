# Story 1.2 — Autenticação Multi-Tenant e Estrutura de Workspace

## Status

**Status:** Done
**Epic:** 01 — Fundação e Navegação
**Wave:** 2 (paralelo com 1.3 — sem conflito de arquivos)
**Depende de:** Story 1.1 (monorepo + infra base)
**Criada em:** 2026-06-25

## Executor Assignment

| Role | Agent | Notes |
|------|-------|-------|
| Executor | @dev (Dex) | Auth + multi-tenant |
| Quality Gate | @architect (Aria) | Validação de RLS e isolamento |

---

## Story

**As a** gestor de tráfego,
**I want to** criar uma conta, fazer login e ter meu workspace criado automaticamente,
**so that** todas as minhas ações sejam isoladas dos dados de outros usuários desde o primeiro acesso.

---

## Acceptance Criteria

- [x] **AC 1.2.1** — Supabase Auth configurado com provedores: email/password e magic link. Tabelas de usuário usam `auth.users` do Supabase (sem tabela `users` customizada).
- [x] **AC 1.2.2** — Trigger `auto_create_workspace_settings` (já na migração 1.1) funciona: ao criar um workspace, `workspace_settings` é criado automaticamente com `workspace_id` e `classification_confidence_threshold = 0.700` (default do schema — conforme `docs/architecture.md` Section 9). Nenhum campo extra.
- [x] **AC 1.2.3** — Ao registrar novo usuário (primeiro acesso), um workspace é criado automaticamente com `name = 'Meu Workspace'` e o usuário é adicionado como `role = 'owner'` em `workspace_members`. *Decisão interativa: fluxo Register → /onboarding (nomear) — "Meu Workspace" é valor default pré-preenchido. Workspace criado em onboarding, não no register.*
- [x] **AC 1.2.4** — Proxy Next.js (`apps/web/src/proxy.ts`) protege todas as rotas dentro de `(dashboard)/` — redireciona para `/login` se não autenticado. **Next.js 16 renomeou `middleware.ts` → `proxy.ts`** (middleware.ts gera warning de deprecação; ambos coexistindo lança erro). Proxy roda exclusivamente em Node.js runtime — `export const runtime = 'edge'` é proibido e lança erro em build. *Confirmado no build: "ƒ Proxy (Middleware)" detectado.*
- [x] **AC 1.2.5** — Rotas: `/login` (email + magic link), `/register`, `/onboarding` (criação de workspace com nome personalizado, se usuário ainda não tiver workspace).
- [x] **AC 1.2.6** — Server-side: `createSupabaseServerClient()` em `packages/database/src/server.ts` — usa `@supabase/ssr` para cookies. Client-side: `createSupabaseBrowserClient()` em `packages/database/src/browser.ts`.
- [x] **AC 1.2.7** — RLS ativa em todas as tabelas: `workspaces`, `workspace_members`, `workspace_settings`. Policy `workspace_member_access`: usuário só acessa rows onde seu `auth.uid()` consta em `workspace_members` para o `workspace_id`.
- [x] **AC 1.2.8** — `workspace_id` está disponível no contexto de toda a aplicação via Zustand store `useWorkspaceStore` com `workspaceId`, `workspaceName`, `role`.
- [x] **AC 1.2.9** — Testes: 4/4 passando — workspace actions (unauthenticated redirect, existing workspace redirect) + store (setWorkspace, clearWorkspace).
- [x] **AC 1.2.10** — Nenhuma secret é exposta em variáveis `NEXT_PUBLIC_*` além de `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY`.

---

## Tasks

- [x] **T1** — Instalar `@supabase/supabase-js` e `@supabase/ssr` em `packages/database`. Criar `packages/database/src/server.ts` e `packages/database/src/browser.ts` com os dois clients (server usa cookies, browser usa localStorage). Exportar ambos em `packages/database/src/index.ts`.
- [x] **T2** — Criar migração `supabase/migrations/20260101000001_rls_policies.sql` com policies RLS para `workspaces`, `workspace_members`, `workspace_settings`.
- [x] **T3** — Criar `apps/web/src/proxy.ts` (não `middleware.ts`) — verificar sessão Supabase, redirecionar `/login` se não autenticado para rotas `(dashboard)/`. Exportar como `export async function proxy`. Node.js runtime — sem `export const runtime`.
- [x] **T4** — Criar páginas de auth: `apps/web/src/app/(auth)/login/page.tsx`, `/(auth)/register/page.tsx`, `/(auth)/onboarding/page.tsx`. Usar ShadCN Card + Input + Button.
- [x] **T5** — Criar Server Action `createWorkspace(formData)` em `apps/web/src/app/actions/workspace.ts`. Cria workspace + adiciona membro owner em sequência.
- [x] **T6** — Criar Zustand store `useWorkspaceStore` em `apps/web/src/stores/workspace.ts` com `workspaceId`, `workspaceName`, `role`. Hidratado via `WorkspaceProvider` no `(dashboard)/layout.tsx`.
- [x] **T7** — Testes: 4/4 passando (`src/__tests__/workspace-actions.test.ts`).
- [x] **T8** — Lint ✅ · TypeCheck ✅ · Build ✅ (8 páginas) · Tests 4/4 ✅

---

## Dev Notes

### Client Supabase (Referência: `docs/architecture.md` Section 11)

```typescript
// packages/database/src/server.ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export function createSupabaseServerClient() {
  const cookieStore = cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (cookiesToSet) => {
          cookiesToSet.forEach(({ name, value, options }) =>
            cookieStore.set(name, value, options)
          )
        },
      },
    }
  )
}
```

### Fluxo de Criação de Workspace

```sql
-- Server Action deve executar em sequência ou via RPC:
INSERT INTO workspaces (name, created_by) VALUES ($1, auth.uid()) RETURNING id;
INSERT INTO workspace_members (workspace_id, user_id, role) VALUES ($workspaceId, auth.uid(), 'owner');
-- workspace_settings criado automaticamente via trigger (Story 1.1)
```

### RLS Policies (migração 20260101000001_rls_policies.sql)

```sql
-- workspaces: usuário pode ver apenas workspaces onde é membro
CREATE POLICY "workspace_member_access" ON workspaces
  FOR ALL USING (
    id IN (
      SELECT workspace_id FROM workspace_members WHERE user_id = auth.uid()
    )
  );

-- workspace_members: pode ver apenas seus próprios registros no workspace
CREATE POLICY "own_workspace_members" ON workspace_members
  FOR ALL USING (user_id = auth.uid());

-- workspace_settings: herda acesso via workspace membership
CREATE POLICY "workspace_settings_access" ON workspace_settings
  FOR ALL USING (
    workspace_id IN (
      SELECT workspace_id FROM workspace_members WHERE user_id = auth.uid()
    )
  );
```

### Proxy Pattern (Next.js 16 — substitui middleware.ts)

> ⚠️ Next.js 16: arquivo é `proxy.ts`, não `middleware.ts`. Named export é `proxy`, não `middleware`. Node.js runtime APENAS — não declarar `export const runtime`. A Edge Function de tracking (`/t/[code]/route.ts`) é um route handler separado e NÃO é afetada por essa mudança — são dois conceitos distintos.

```typescript
// apps/web/src/proxy.ts
import { createServerClient } from '@supabase/ssr'
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export async function proxy(request: NextRequest) {
  // Usar updateSession do @supabase/ssr para refresh automático de tokens
  // Redirecionar para /login se não autenticado e rota começa com /(dashboard)/
}

// Alternativa: export default function proxy(request: NextRequest) { ... }

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
}
```

### Zustand Store

```typescript
// apps/web/src/stores/workspace.ts
import { create } from 'zustand'

interface WorkspaceState {
  workspaceId: string | null
  workspaceName: string | null
  role: 'owner' | 'admin' | 'member' | null
  setWorkspace: (workspace: { id: string; name: string; role: string }) => void
}
```

### Segurança

- `SUPABASE_SERVICE_ROLE_KEY` — NUNCA exposta no client. Apenas em Server Actions e API Routes.
- CI deve bloquear `NEXT_PUBLIC_*SERVICE_ROLE*` e `NEXT_PUBLIC_*SECRET*` (conforme `docs/architecture.md` Section 15).

---

## Testing

- **Unit:** Mock Supabase client de `packages/mocks` para testes de Server Actions
- **Integration:** Testar fluxo completo de criação de conta → workspace → login → acesso a rota protegida
- **Security:** Verificar que RLS impede acesso cross-tenant (usuário A não vê dados de workspace B)

---

## Change Log

| Data | Autor | Ação |
|------|-------|------|
| 2026-06-25 | Morgan (@pm) | Story criada para execução do Epic 1 |
| 2026-06-25 | Morgan (@pm) | Ready — Wave 1 gate PASS; Wave 2 liberada; modo interactive |
| 2026-06-25 | Dex (@dev) | InProgress → InReview — todos os ACs concluídos; lint+typecheck+build+tests passando |
| 2026-06-25 | Aria (@architect) | QA Gate PASS — blocker (EmptyState 'use client') corrigido; Wave 2 aprovada |
| 2026-06-25 | Morgan (@pm) | Done — Wave 2 gate PASS confirmado; Wave 3 liberada |

---

## Dev Agent Record

**Implementado por:** @dev (Dex) — modo interactive (3 checkpoints)
**Branch:** master
**Commit:** pendente (@devops após QA gate)
**Decisões interativas:**
- proxy.ts confirmado (Next.js 16) — build confirmou "ƒ Proxy (Middleware)"
- Fluxo Register → /onboarding (nomear workspace) — "Meu Workspace" como default
- Zustand instalado em apps/web apenas (não compartilhado)
- createSupabaseServerClient() é async (Next.js 16 — cookies() retorna Promise)
- next adicionado como peerDependency + devDependency em packages/database para resolução de tipos

---

## QA Results

**QA Gate:** —
**Verdict:** —
