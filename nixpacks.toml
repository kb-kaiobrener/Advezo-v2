[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @advezo/whatsapp-worker build"]

[start]
cmd = "pnpm --filter @advezo/whatsapp-worker start"
